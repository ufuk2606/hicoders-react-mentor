import { renderNav, renderContent, renderFooter } from "./function.js";
/**
 * Importing these functions from function.js and rendering.
 */
(function render() {
  renderNav();
  renderContent();
  renderFooter();
}());
