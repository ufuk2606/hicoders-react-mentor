// you can create a variable called BASE_URL that stores => (http://dev.hicoders.cloud/playground/api/v1/User) and export it here and use it in your components
/// so you don't have to type the url manually everywhere in your code
