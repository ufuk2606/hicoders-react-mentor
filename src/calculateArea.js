export const areaOfTriangle = (pSide1,pSide2) => (pSide1*pSide2) / 2 ;

export const areaOfRectangle = (pSide1,pSide2) => pSide1*pSide2;

export const areaOfCircle = (pRadius) => Number((Math.PI*Math.pow(pRadius, 2)).toFixed(2));
